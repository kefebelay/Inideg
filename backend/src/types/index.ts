export interface Item {
    _id?: string;
    name: string;
    description: string;
    price: number;
}

export interface User {
    _id?: string;
    username: string;
    password: string;
    email: string;
    age:string;
}